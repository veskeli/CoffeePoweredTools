import React from 'react';
import { Button } from "@/components/ui/button"

const Settings = () => {
  return (
    <div>
      <h2>Settings</h2>
      <Button>Testing button</Button>
      <p>This is the settings page. You can add your settings here.</p>
    </div>
  );
};

export default Settings;